/* import axios from "axios";

let axiosInstance = axios.create({
  baseURL: `http://localhost:8081/tracks`,
  responseType: "stream"
});

function getAudio(id) {
  return axiosInstance.get(id);
}

export default getAudio;
 */
